/**
 * Q1 Stabilization — Phase 5 (Test Automation). Vitest global setup: wires
 * jest-dom's matchers (toBeInTheDocument, etc.) into Vitest's `expect`.
 */
import '@testing-library/jest-dom/vitest';
